import { SortByGymCountPipe } from './sort-by-gym-count-pipe';

describe('SortByGymCountPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByGymCountPipe();
    expect(pipe).toBeTruthy();
  });
});
