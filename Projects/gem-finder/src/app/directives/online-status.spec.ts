import { OnlineStatus } from './online-status';

describe('OnlineStatus', () => {
  it('should create an instance', () => {
    const directive = new OnlineStatus();
    expect(directive).toBeTruthy();
  });
});
